name: Deploy to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Pull, build and restart via PM2
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          script: |
            set -e
            export PATH="$PATH:/usr/local/go/bin:$HOME/go/bin:$HOME/.nvm/versions/node/v24.18.1/bin:$HOME/.bun/bin"
            cd /srv/apps/apsthira
            git pull origin main
            ./build.sh
            ./pm2.sh
